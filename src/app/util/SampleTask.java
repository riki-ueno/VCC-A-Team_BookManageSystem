package app.util;

import java.util.TimerTask;

public class SampleTask extends TimerTask {

	public SampleTask() {
	}

	@Override
	public void run() {
		// System.out.println("あ");
		// try {
		// LocalDateTime tryTimeStartMail = null;// 今日の日付＋指定時間
		// LocalDateTime tryTimeEndMail = null;// ↑の日付＋トライ時間
		// LocalDateTime nowTime = LocalDateTime.now();
		//
		// // 処理(開始時間から、一定時間内リトライを行う)
		// if (nowTime.isAfter(tryTimeStartMail) &&
		// nowTime.isBefore(tryTimeEndMail)) {
		// // 一回成功したら次は処理しないように記述する
		// }
		// } catch (Throwable ex) {
		// }
	}

}
